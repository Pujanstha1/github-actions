name: Env Config 
on: workflow_dispatch
env:
    APP_ENV: "dev"
jobs:
    setting_env:
        runs-on: ubuntu-latest 
        steps:
            - name: Importing Env Configurations
              run: echo "App is connecting to $APP_ENV"